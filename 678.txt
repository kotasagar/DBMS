CREATE VIEW EMP_VIEW AS (SELECT E.SSN,E.FNAME,E.LNAME,D.DNAME FROM EMPLOYEE E,DEPARTMENT D);
CREATE VIEW DEPT_VIEW AS (SELECT DNO,DLOC FROM DLOCATIONS);
CREATE VIEW MANAGER AS (SELECT FNAME,LNAME FROM EMPLOYEE WHERE SSN=SUPERSSN);